// DDNS 更新設定
#define DUC_SERVICE true // 是否啟用ddns更新服務
const char domainType[] = "noip";            //更新的DDNS服務
const char domainName[] = "my-no-ip-domain"; //更新的DDNS網域名稱
const char domainUser[] = "noip-user";       //更新的DDNS網域帳號
const char domainPass[] = "p@ssw0rd";        //更新的DDNS網域密碼

// OLED 設定
#define OLED_DISPLAY true // 是否啟用OLED顯示狀態

// WiFi連線設定
#define WIFI_SSID "wifi"
#define WIFI_PSK  "p@ssw0rd"

// 網站帳密設定
#define USERNAME "admin"
#define PASSWORD "p@ssw0rd"

// 裝置IP與MAC位置
#define DEVICE_COUNT 1 // 定義裝置數量 (用於Ping和網頁生成)
const char *MACAddress[] = {"00:00:00:00:00:00"};
const IPAddress ip[]  =    {IPAddress(192, 168, 1, 100)};

// 憑證
#ifndef CERT_H_
#define CERT_H_
unsigned char example_crt_DER[] = {

};
unsigned int example_crt_DER_len = 0;
#endif

// 憑證私鑰
#ifndef PRIVATE_KEY_H_
#define PRIVATE_KEY_H_
unsigned char example_key_DER[] = {
  
};
unsigned int example_key_DER_len = 0;
#endif